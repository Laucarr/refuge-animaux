{% extends 'base.html.twig' %}

{% block title %}Modifier l'adoption
{% endblock %}

{% block body %}
	<div class="row justify-content-center">
		<div class="col-lg-7 col-xl-6">
			<div class="card shadow-sm">
				<div class="card-header bg-light">
					<h1 class="mb-0 fw-bold">Modifier l'adoption</h1>
				</div>
				<div class="card-body">
					{{ include('adoption/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
				</div>
			</div>
			<div class="mt-3 d-flex justify-content-between">
				<a href="{{ path('app_adoption_index') }}" class="btn btn-outline-secondary">← Retour à la liste</a>
				{{ include('adoption/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}
