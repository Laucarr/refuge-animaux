<form method="post" action="{{ path('app_caretaker_delete', {'id': caretaker.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ caretaker.id) }}">
    <button class="btn">Delete</button>
</form>
