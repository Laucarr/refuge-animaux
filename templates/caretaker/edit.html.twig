{% extends 'base.html.twig' %}

{% block title %}Edit Caretaker{% endblock %}

{% block body %}
    <h1>Edit Caretaker</h1>

    {{ include('caretaker/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_caretaker_index') }}">back to list</a>

    {{ include('caretaker/_delete_form.html.twig') }}
{% endblock %}
